/**
 * Imágenes del sitio (local optimized WebP y assets del store).
 *
 * Historial: este archivo contenía cubiertas de colecciones (COLLECTION_*),
 * imágenes de producto (IMG_*) y un mapa de productos a imágenes
 * (PRODUCT_IMAGES). Todos quedaron sin uso tras migrar a
 * `lib/collection-images.ts` y a los catálogos con imagen propia;
 * se eliminaron en la auditoría de código muerto (recuperables en git).
 */
export const HERO_GROCERY = "/images/hero-grocery.webp"
